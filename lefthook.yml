output:
  - execution
  - summary
pre-commit:
  commands:
    fmt:
      run: cargo +nightly fmt --all
    lint:
      run: cargo +nightly clippy --all-targets --all-features -- -D warnings
post-checkout:
  parallel: true
  jobs:
    - script: "cleanup.cjs"
      runner: node
