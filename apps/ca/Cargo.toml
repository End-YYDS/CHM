[package]
name = "ca"
version.workspace = true
edition.workspace = true

[dependencies]
grpc = { path = "../../libs/grpc", features = ["ca_server"] }
tokio = { version = "1.43.0", features = ["macros", "rt-multi-thread"] }
config = { path = "../../libs/config" }
